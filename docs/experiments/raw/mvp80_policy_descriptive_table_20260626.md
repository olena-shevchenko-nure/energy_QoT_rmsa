| Policy | Type | Learning signal | Uses GNN/CNN? | Uses override? | Runtime action |
| --- | --- | --- | --- | --- | --- |
| Calibrated DQN-Override baseline ranker | DQN-like candidate ranker / calibrated override policy | Distillation from the old10 tree-ranker family with windowed-return / advantage-style candidate targets and override-rate calibration | No | Yes | Starts from the baseline candidate decision and applies a learned candidate override only at the calibrated override rate; action is the selected feasible Top-N candidate |
| LightGBM-override ranker | Gradient-boosted candidate ranker | LambdaRank-style supervised ranking on candidate features, trained around hard/exception cases where an override can improve the baseline decision | No | Yes | Scores feasible Top-N candidates with LightGBM and overrides the baseline decision only in the configured old10 candidate regime |
| GNN-CNN Full DQN Ranker | Full neural DQN candidate ranker | Distillation/listwise candidate ranking and DQN-style value learning over the shared Top-N candidate surface | Yes | No | Directly ranks all feasible Top-N candidates with the neural Q-score and selects the highest-scoring valid candidate |
| XLRON Counterfactual Ranker | XLRON-inspired graph-transformer actor/ranker with counterfactual ranking calibration | PPO warm-start / online trajectory training followed by student-state distillation and counterfactual/listwise ranking signals | Uses transformer with graph/link-token and optional spectrum CNN encoders; not the GNN-CNN stack | Bucket guard, not baseline override | Scores Top32 feasible candidates with the XLRON neural policy/ranker and selects the best valid candidate subject to the bucket guard |
| A3C Policy Distilled from Full DQN | GNN-CNN actor-critic policy distilled from Full DQN | Actor-critic / A3C-style policy with distillation targets from the Full DQN teacher and value-head training | Yes | No | Actor outputs a masked distribution over feasible Top-N candidates; runtime takes the highest-probability valid candidate |
| Energy-Aware-KSP-BM-FF | Deterministic energy-aware heuristic | No learning; explicit hand-designed candidate score using KSP, best modulation, first-fit, energy and fragmentation terms | No | No | Applies explicit rule-based scoring over feasible candidates and selects the best energy-aware KSP-BM-FF candidate |
| KSP-FF | Deterministic heuristic | No learning; shortest-path routing with first-fit spectrum assignment | No | No | Selects the first feasible first-fit spectrum allocation on KSP-generated routes |
| KSP-BM-FF | Deterministic heuristic | No learning; KSP with best modulation and first-fit spectrum assignment | No | No | Selects the first feasible candidate after KSP route generation and best-modulation filtering |
